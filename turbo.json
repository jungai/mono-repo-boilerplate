{
	"$schema": "https://turborepo.org/schema.json",
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".svelte-kit/**"]
		},
		"start": {
			"dependsOn": ["^build", "^start"],
			"outputs": ["dist/**"]
		}
	}
}
